name: Gitleaks

on:
  push:
    branches: 
       - main
       - staging
  pull_request:
    branches: 
       - main
       - staging  
 

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: gitleaks-action-checking-key
      uses: zricethezav/gitleaks-action@master
